---
import { Sparkles } from 'lucide-react'
import ServicePhases from './ServicePhases'
---

<section id="servicios" class="relative w-full bg-(--surface-primary)">
  <!-- Glow -->
  <div
    class="absolute left-0 bottom-0 w-[50%] h-120 rounded-full rotate-180
           bg-linear-to-br
           from-(--color-primary)
           to-(--color-secundary)
           blur-[70px] opacity-12 pointer-events-none"
    aria-hidden="true"
  >
  </div>
  <!-- Stars -->
  <img
    src="/start.svg"
    alt={`icon-star`}
    class="star-twinkle w-4 h-4 absolute left-0 md:left-20 top-60"
    style="animation-delay: 0s;"
    aria-hidden="true"
  />
  <img
    src="/start.svg"
    alt={`icon-star`}
    class="star-twinkle w-3 h-3 absolute right-0 md:right-20 top-50"
    style="animation-delay: 0.6s;"
    aria-hidden="true"
  />
  <img
    src="/start.svg"
    alt={`icon-star`}
    class="star-twinkle w-3 h-3 absolute left-1/2 -translate-x-1/2 top-140"
    style="animation-delay: 1.1s;"
    aria-hidden="true"
  />

  <main class="mx-auto max-w-7xl">
    <!-- Header -->
    <div class="text-center md:mb-16">
      <span class="inline-flex items-center gap-2 text-sm uppercase text-(--color-primary)">
        <Sparkles className="w-4 h-4" />
        Servicios
      </span>

      <h2 class="mt-4 text-3xl md:text-5xl font-semibold text-(--text-primary)">
        Cómo te acompañamos
      </h2>

      <p class="mt-4 max-w-2xl mx-auto text-(--text-secundary)">
        Un proceso estratégico, iterativo y enfocado en resultados reales.
      </p>
    </div>

    <!-- Content -->
    <ServicePhases client:load />
  </main>
</section>

<style>
  @keyframes starTwinkle {
    0%,
    100% {
      transform: scale(0.7);
      filter: drop-shadow(0 0 0 rgba(255, 255, 255, 0));
      opacity: 0.75;
    }
    50% {
      transform: scale(1.2);
      filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.6));
      opacity: 1;
    }
  }

  .star-twinkle {
    animation: starTwinkle 2.6s ease-in-out infinite;
    transform-origin: center;
  }
</style>
